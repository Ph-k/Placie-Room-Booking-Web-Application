spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://localhost:3306/placiedb?serverTimezone=UTC

# Change to your settings
spring.datasource.username=root
spring.datasource.password=root

# SSL
server.port=8443
server.ssl.key-store=classpath:server3.p12
server.ssl.key-store-password=EncryptionSuperKey

# JKS or PKCS12
server.ssl.keyStoreType=PKCS12
# Spring Security
security.require-ssl=true

#Setting maximum file size of uploaded files to 10MB
spring.servlet.multipart.max-file-size=10000000B

